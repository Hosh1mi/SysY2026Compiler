// test_inline.sy
int add(int a, int b) {
    if (a > 0)
        return a + b;
    else
        return a - b;
}

int main() {
    int x = getint();
    int y = getint();
    int z = add(x, y); 
    int w = add(y, x); 
    return z + w;
}